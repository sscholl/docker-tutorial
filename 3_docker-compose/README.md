Build image and start containers
```
docker-compose up -d
```

Go to http://localhost/ in browser.


Show volumes
```
docker volume ls
```


Cleanup
```
docker-compose stop
docker-compose rm -f
```
